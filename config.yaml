server:
  port: 8080
  host: 0.0.0.0
  web_path: ""  # Web 界面路径，留空为根路径 /，设置为 /web 则访问 /web/

auth:
  enabled: true
  api_keys:
    - ${API_KEY}

templates:
  dir: ./templates
  default: default

clash:
  default_rules: default
